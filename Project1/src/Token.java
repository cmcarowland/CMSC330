/**
 * Raymond Rowland
 * Project 1
 * 20 OCT 24
 *
 * Enum representing various tokens used in lexical analysis.
 * Each token corresponds to a specific keyword, symbol, or 
 * identifier recognized by the lexer, facilitating tokenization 
 * of the input data. 
*/

// Enumerated type that defines the list of tokens

enum Token {AT, COLOR, END,	HEIGHT, RECTANGLE, RIGHT_TRIANGLE, SCENE, WIDTH, COMMA, SEMICOLON, PERIOD, LEFT_PAREN,
    RIGHT_PAREN, IDENTIFIER, NUMBER, EOF, ISOSCELES, PARALLELOGRAM, REGULARPOLYGON, TEXT, OFFSET, SIDES, RADIUS, QUOTE}